name: marketing
version: '1.0.0'
config-version: 2
profile: default

model-paths: ["models"]
seed-paths: ["seeds"]

seeds:
  marketing:
    +full_refresh: false
    +meta:
      owner: "marketing"
      criticality: "CORE"

models:
  marketing:
    +materialized: table
    +meta:
      owner: "marketing"
      criticality: "CORE"
